import { FueAvatarComponent } from "./avatar.component";

describe("ButtonDirective", () => {
  it("should create an instance", () => {
    const directive = new FueAvatarComponent();
    expect(directive).toBeTruthy();
  });
});
